version: "3"

services:
  pvs_surl:
    build:
      context: .
    ports:
      - 5000:5000
  mariadb:
    image: mariadb:latest
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=TRUE
    ports:
      - 3306:3306

